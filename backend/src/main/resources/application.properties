spring.application.name=distrofy-backend
# Configuraci�n de la base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/distrofy
spring.datasource.username=distrofy_user
spring.datasource.password=jeanjean123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# Configuraci�n del servidor
server.port=8080
server.servlet.context-path=/api

# Configuraci�n de seguridad JWT (valores iniciales para desarrollo)
jwt.secret=clave_secreta_muy_segura_para_desarrollo_local_cambiar_en_produccion
jwt.expiration=86400000

# Configuración de validación
spring.jpa.defer-datasource-initialization=true

# Configuración de logging
logging.level.com.distrofy.backend=DEBUG
logging.level.org.springframework.security=DEBUG